name: neuro_lawyer
description: "Приложение 'Нейроюрист': голосовой ассистент по законам РФ."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '^3.9.0'
  flutter: ">=3.35.0"

dependencies:
  flutter:
    sdk: flutter
  speech_to_text: ^6.6.2  # Для голосового ввода

flutter:
  uses-material-design: true